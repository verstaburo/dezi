mixin anchors(data)
  +b.UL.anchors&attributes(attributes)
    each item in data
      +e.LI.item
        +e.A.link.js-section-anchor(href=item.anchor data-section-target=item.section class=item.active)
          +icon('circle').__border
          +e.text!= item.name
    block
